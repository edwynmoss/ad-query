# Build output
app/build/bin/
app/frontend/dist/
app/frontend/node_modules/
node_modules/

# Go
*.exe
*.test
*.out

# Wails

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local config / secrets
*.local
.env

# Data exports — may contain real directory / Microsoft 365 data. Never commit.
# (Use `git add -f` only for a deliberately-synthetic sample.)
*.xlsx
*.xls
*.csv

# Editor and assistant scratch config. Local to whoever is working on the
# machine, useful to nobody cloning this, and settings.local.json records the
# machine's own domain, which is not ours to publish.
.claude/
